# Dependencies
node_modules/

# Build output (packages/strk20-pay/dist IS tracked — GitHub Pages serves the demo from it)
build/
.next/
.turbo/
.vercel/
coverage/

# Environment / secrets — NEVER commit (public repo!)
.env
.env.*
*.local
*.key
*.pem
id_rsa*
wallet.json
*mnemonic*
*seed*

# OS / editor
.DS_Store
Thumbs.db
*.log

# Binaries
*.exe
*.dll

# Watcher state: real invoice ledgers, never committed
server/watcher/invoices.json
*.invoices.json

# Video build artefacts. The narration mp3 is committed so the script's voice is
# reviewable; the 33MB wav and the per-line pieces rebuild from it.
docs/narration/
docs/demo.narration.wav

# Screen recordings and the scratch state a shoot produces. The finished
# video lives on YouTube and its URL is in strk20.json; the raw takes are
# large, are re-shot rather than edited, and are nobody else's business.
docs/recording/

# Internal working notes: never publish these in a public submission repo
PROGRESS.md
CLAUDE.md
AGENTS.md
