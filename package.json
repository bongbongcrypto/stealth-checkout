{
  "name": "stealth-checkout",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "tsc -p packages/strk20-pay/tsconfig.json",
    "test": "npm run build && node --test \"packages/strk20-pay/test/*.test.mjs\" \"server/watcher/test/*.test.mjs\"",
    "e2e:watcher": "node server/watcher/e2e-live.mjs"
  },
  "devDependencies": {
    "esbuild": "0.25.9",
    "typescript": "5.9.2"
  }
}
